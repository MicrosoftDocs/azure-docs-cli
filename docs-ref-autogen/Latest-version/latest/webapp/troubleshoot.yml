### YamlMime:AzureCLIGroup
uid: az_webapp_troubleshoot
name: az webapp troubleshoot
summary: |-
  Diagnose common Linux web app problems.
description: |-
  Preview command group that pairs built-in configuration checks (from KuduLite on the worker) with per-instance runtime status and startup summaries from ARM. Use when a Linux app is failing to start, returning HTTP 502/503, or exhibiting other post-deployment misbehavior.
status: Preview
isPreview: true
previewOrExperimentalInfo: 'This command group is in preview and under development. Reference and support levels: https://aka.ms/CLI_refstatus'
sourceType: Core
directCommands:
- uid: az_webapp_troubleshoot_status
  name: az webapp troubleshoot status
  summary: |-
    Show site runtime status and recent startup summary for a Linux web app.
  description: |-
    Aggregates two data sources:

      - Site Runtime Status
      - Startup summary: KuduLite (SCM) /api/startuplogs/summary (counts of
        successful and failed startup attempts in the last 24h, plus the
        most recent success and failure timestamps).

    Use --instance to scope both to a single worker. By default the command
    returns a structured payload so the standard `-o json/yaml/table` formatters
    handle output. Pass `--report` to
    print a human-readable two-section report to stdout instead.
  status: Preview
  isPreview: true
  previewOrExperimentalInfo: "Command group 'webapp troubleshoot' is in preview and under development. Reference and support levels: https://aka.ms/CLI_refstatus"
  sourceType: Core
  syntax: >-
    az webapp troubleshoot status --name
                                  --resource-group
                                  [--acquire-policy-token]
                                  [--change-reference]
                                  [--instance]
                                  [--report {false, true}]
                                  [--slot]
  examples:
  - summary: |-
      Show status for all instances of a web app (JSON by default)
    syntax: az webapp troubleshoot status --name MyWebApp --resource-group MyResourceGroup
  - summary: |-
      Print the human-readable report
    syntax: az webapp troubleshoot status --name MyWebApp --resource-group MyResourceGroup --report
  - summary: |-
      Show status scoped to a single worker instance
    syntax: az webapp troubleshoot status --name MyWebApp --resource-group MyResourceGroup --instance 7c2d9
  requiredParameters:
  - isRequired: true
    name: --name -n
    summary: |-
      Name of the web app. If left unspecified, a name will be randomly generated. You can configure the default using `az configure --defaults web=<name>`.
  - isRequired: true
    name: --resource-group -g
    summary: |-
      Name of resource group. You can configure the default group using `az configure --defaults group=<name>`.
  optionalParameters:
  - name: --acquire-policy-token
    summary: |-
      Acquiring an Azure Policy token automatically for this resource operation.
    groupName: Global Policy Arguments
  - name: --change-reference
    summary: |-
      The related change reference ID for this resource operation.
    groupName: Global Policy Arguments
  - name: --instance
    summary: |-
      Scope the report to a single worker instance.
    description: |-
      Accepts either the hex instanceId (from `az webapp list-instances`) or the machine name (e.g. `lw0sdlwk0007AB`). When omitted, returns an overview of every instance seen in the last 24 hours.
  - name: --report
    defaultValue: "False"
    acceptedValues: false, true
    summary: |-
      Print a human-readable, color-coded report instead of returning structured data.
    description: |-
      When set, the command writes a formatted report (overview table plus per-instance Last runtime status and Startup summary) to stdout and returns no machine-readable output. Omit --report to keep the default structured payload that works with `-o json`, `-o yaml`, and `-o table`.
  - name: --slot -s
    summary: |-
      The name of the slot. Defaults to the production slot if not specified.
  globalParameters:
  - name: --debug
    defaultValue: "False"
    summary: |-
      Increase logging verbosity to show all debug logs.
  - name: --help -h
    summary: |-
      Show this help message and exit.
  - name: --only-show-errors
    defaultValue: "False"
    summary: |-
      Only show errors, suppressing warnings.
  - name: --output -o
    defaultValue: json
    acceptedValues: json, jsonc, none, table, tsv, yaml, yamlc
    summary: |-
      Output format.
  - name: --query
    summary: |-
      JMESPath query string. See http://jmespath.org/ for more information and examples.
  - name: --subscription
    summary: |-
      Name or ID of subscription. You can configure the default subscription using `az account set -s NAME_OR_ID`.
  - name: --verbose
    defaultValue: "False"
    summary: |-
      Increase logging verbosity. Use --debug for full debug logs.
commands:
- az_webapp_troubleshoot_collect
- az_webapp_troubleshoot_collect_network-capture
- az_webapp_troubleshoot_status
metadata:
  description: Preview command group that pairs built-in configuration checks (from KuduLite on the worker) with per-instance runtime status and startup summaries from ARM. Use when a Linux app is failing to start, returning HTTP 502/503, or exhibiting other post-deployment misbehavior.
